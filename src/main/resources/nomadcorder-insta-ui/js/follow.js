function follow(check, userId){    // true -> follow 하기    // false -> unFollow 하기    let url = "/follow/"+userId;    if(check){        fetch(url,{            method: "POST"        }).then(function(res){            return res.text();        }).then(function(res){            if(result === "ok"){                let follow_check_el = document.querySelector("#follow_check");                follow_check_el.innerHTML="<button onclick='follow(false,"user_id+")' class='profile_edit_btn'> 팔로잉</button>";            }        });    }else{        fetch(url,{            method: "DELETE"        }).then(function(res){            return res.text();        }).then(function(res){            if(result === "ok"){                let follow_check_el = document.querySelector("#follow_check");                follow_check_el.innerHTML="<button onclick='follow(true,"user_id+)' class='profile_follow_btn'> 팔로</button>";    }});}}